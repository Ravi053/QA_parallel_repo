pipeline {
  agent any
  stages {
    stage('build') {
      step {
        echo "this is build stage"
        
      }
    }
    stage('Deploy') {
      step {
        echo "this is deploy stage"
  }
}
  }
}
