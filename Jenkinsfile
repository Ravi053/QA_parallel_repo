pipeline {
    agent any
    stages {
        
        stage('build') {
            step {
                echo "this is build stage"
                sh '''
                sleep 3
                '''
            }
        }
    }
}
