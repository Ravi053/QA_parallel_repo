pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "this is build stage"
            }
        }
        stage('Print') {
            steps {
                echo "this is build stage"
    }
}
          stage('Node') {
             steps {
                echo "this is build stage"
                
            }
          }
    }
}
